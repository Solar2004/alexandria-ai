# ── Config personal de Claude Code (se regenera con `alx setup`) ──
.claude/
.remember/
CLAUDE.md

# ── Build / runtime ──
target/
proyecto-final/alexandria/target/
state/
**/state/
*.jsonl

# ── Secretos ──
.env
.env.*
*.key
*sk-REPLACE_ME*
# Config de MCPs con API keys locales (regenerar con alx setup)
config/mcp-install.json
plugins/alexander-harness/.mcp.json

# ── Backups / caches ──
backups/
*.bak*
.qdrant_code_embeddings/
.playwright-mcp/
.cgr-*
__pycache__/
skills.old/

# ── Node ──
node_modules/
package-lock.json

# ── Plan media ──
plan/media/
night-ops/report/

# ── Artefactos del pipeline (alx feature) ──
artifacts/
