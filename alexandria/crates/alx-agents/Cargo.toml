[package]
name = "alx-agents"
version.workspace = true
edition.workspace = true
description = "Registry, router, spawn, headless sessions"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
