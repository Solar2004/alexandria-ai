[package]
name = "alx-bench"
version.workspace = true
edition.workspace = true
description = "Metricas perf, umbrales, diff check"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
