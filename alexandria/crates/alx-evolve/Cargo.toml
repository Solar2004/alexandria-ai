[package]
name = "alx-evolve"
version.workspace = true
edition.workspace = true
description = "Harness evolutivo: la AI crea harnesses en tiempo real, temporal/permanente, doc-min, watcher de objetivos"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
