[package]
name = "alx-gate"
version.workspace = true
edition.workspace = true
description = "Verificacion: build/test/lint, LSP discovery, evidencia"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
