[package]
name = "alx-governor"
version.workspace = true
edition.workspace = true
description = "Gobernador de coste: routing dificultad a tier, budget, ledger"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
