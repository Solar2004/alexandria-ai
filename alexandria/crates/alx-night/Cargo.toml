[package]
name = "alx-night"
version.workspace = true
edition.workspace = true
description = "Scheduler autonomo: cron, informe, commit atomico"

[dependencies]
alx-core = { path = "../alx-core" }
alx-task = { path = "../alx-task" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
