# Doc-min: compuerta de evidencia — sin pruebas reales, la fase no pasa.
# Conectado: `alx build` es real (gate cargo build del motor, exit 1 si falla).
id = "gate.verify"
event = "PhasePassed"
priority = "Pre"
command = "alx build"
timeout_ms = 120000
lock = true
retry = 1
enabled = true
description = "Exige evidencia real del trabajo de la fase: build del motor con exit 0; con política strict, sin evidencia la fase no pasa."
