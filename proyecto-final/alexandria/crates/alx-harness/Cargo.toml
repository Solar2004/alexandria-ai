[package]
name = "alx-harness"
version.workspace = true
edition.workspace = true
description = "Pipeline de fases: contratos, compuertas, reanudable"

[dependencies]
alx-core = { path = "../alx-core" }
alx-task = { path = "../alx-task" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
