[package]
name = "alx-mcp"
version.workspace = true
edition.workspace = true
description = "Server stdio/SSE + client a servidores existentes"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
