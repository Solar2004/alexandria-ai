[package]
name = "alx-memory"
version.workspace = true
edition.workspace = true
description = "Auto-recalls: capturar, comprimir caveman, inyectar"

[dependencies]
alx-core = { path = "../alx-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
