iter = 3
max_iter = 20
work_unit = "ciclo 5 iteracion 3/20: MEJORA REAL critic — de 0% a 5%, seguir subiendo (micro-tareas, criterios, veredictos), re-benchmark"
max_auto = 20
target_iter = 20
awaiting_user = false
