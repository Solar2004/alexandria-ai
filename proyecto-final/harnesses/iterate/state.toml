iter = 5
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 5/20 — verificar night systemd de verdad (run timer), docs por crate indexadas, telemetria visible en tui, pulir feature end-to-end con critic"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
