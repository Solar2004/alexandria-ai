iter = 9
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 9/20 — agentes headless en paralelo (spawn multiple), telemetria avanzada (metricas por crate), verificar tui color, docs de arquitectura actualizadas"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
