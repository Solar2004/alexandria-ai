iter = 4
max_iter = 20
work_unit = "ciclo 3 iteracion 4/20: alx-mcp tools/call ejecuta tools reales del motor, persistencia de tareas (JSONL), verificar feature real end-to-end"
max_auto = 20
target_iter = 20
awaiting_user = false
