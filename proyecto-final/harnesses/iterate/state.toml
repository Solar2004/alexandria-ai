iter = 5
max_iter = 20
work_unit = "ciclo 3 iteracion 5/20: verificar mcp tools reales via alx mcp, feature real end-to-end verificado, integracion tareas persistidas en el pipeline"
max_auto = 20
target_iter = 20
awaiting_user = false
