iter = 2
max_iter = 20
work_unit = "ciclo 6 iteracion 2/20: benchmark con EJECUCION real (compilar/correr el codigo) — el texto no distingue; medir ventaja real del harness"
max_auto = 20
target_iter = 20
awaiting_user = false
