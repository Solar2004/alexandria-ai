iter = 4
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 4/20 — docs por crate (doc-min en libs), night-run usa alx report, telemetria (log de eventos en state/events.log), night scheduler verificado"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
