iter = 10
max_iter = 20
work_unit = "iteracion 10/20: pipeline completo con critic real — alx run --real critica la salida del modelo (Block -> re-itera), derive_must_checks -> memoria, evolve.detect en el trabajo real, informe final por fase"
