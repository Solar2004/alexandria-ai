iter = 1
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 1/20 — TUI de estado del motor (alx tui), documentacion de uso actualizada"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
