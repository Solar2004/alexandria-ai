iter = 12
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 12/20 — optimizar build (release ligero), verificar alx run --real con critic directo, night-report con metrics, resumen de coste (por dia/sesion)"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
