iter = 17
max_iter = 20
work_unit = "iteracion 17/20: alx-agents spawn real — alx agents spawn <nombre> <tarea> ejecuta un agente real (headless) con envelope, alx-critic escalada T3 real, integrar alx con los hooks del sistema (doctor en Stop)"
max_auto = 20
target_iter = 20
awaiting_user = false
