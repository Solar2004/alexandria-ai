iter = 1
max_iter = 20
work_unit = "ciclo 7 iteracion 1/20: reporte VISUAL del benchmark (docs/benchmark-report.html, SVG inline): directa 9/60 vs harness 34/60 (~4x), 3 mecanismos, consistencia, recuperacion por problema. Siguiente: grafico/medicion contra fable 5 si el usuario da acceso; o plan-then-code; o redefinir metrica 5x"
max_auto = 20
target_iter = 20
awaiting_user = false
