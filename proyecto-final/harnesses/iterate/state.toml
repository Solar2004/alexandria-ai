iter = 14
max_iter = 20
work_unit = "iteracion 14/20: experiencia completa — alx run --real como flujo principal con envelope real de agentes, alx-agents spawn, PHALANX como plugin instalable, README de uso, night scheduler systemd"
max_auto = 20
target_iter = 1
awaiting_user = false
