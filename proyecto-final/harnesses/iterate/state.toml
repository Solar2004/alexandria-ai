iter = 10
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 10/20 — verificar agents run en paralelo real, docs de arquitectura (mermaid con estado ciclo 2), pulir spawn (forzar text block directo), night-report con telemetria"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
