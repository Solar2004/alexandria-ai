iter = 8
max_iter = 20
work_unit = "CICLO 2 (pulido): iteracion 8/20 — revisar ideas nuevas (telemetria avanzada, agentes en paralelo headless), pulir run --real (retry de critic con max_tokens mayor), verificar TUI en color real"
max_auto = 20
target_iter = 20
awaiting_user = false
max_auto = 20
target_iter = 20
awaiting_user = false
