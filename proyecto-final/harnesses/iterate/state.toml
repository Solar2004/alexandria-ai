iter = 17
max_iter = 20
work_unit = "ciclo 8 iteracion 17/20: dashboard visual descubrible desde ESTADO.md (a72297f). Deliverable completo: viz verificado + reporte detallado + metrica agregada + suite ejecutable. Siguiente: feedback usuario sobre graficas, run N=186 (coste API), CodeContests"
max_auto = 20
target_iter = 20
awaiting_user = false
