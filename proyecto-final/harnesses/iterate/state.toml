iter = 5
max_iter = 20
work_unit = "ciclo 6 iteracion 5/20: hallazgo — la re-iteracion con feedback textual no corrige bugs de logica; necesita descomposicion en pasos verificables o tests intermedios"
max_auto = 20
target_iter = 20
awaiting_user = false
