iter = 8
max_iter = 20
work_unit = "ciclo 3 iteracion 8/20: cargar agentes reales en el registry (register_from_markdowns), alx agents show <nombre>, cerrar gaps restantes"
max_auto = 20
target_iter = 20
awaiting_user = false
