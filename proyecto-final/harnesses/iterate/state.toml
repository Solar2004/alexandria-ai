iter = 2
max_iter = 20
work_unit = "ciclo 5 iteracion 2/20: MEJORA REAL — critic exito 0%: mejorar micro-tareas demo (concretas) para que el critic apruebe, re-benchmark"
max_auto = 20
target_iter = 20
awaiting_user = false
