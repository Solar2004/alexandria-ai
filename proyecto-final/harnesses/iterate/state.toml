iter = 9
max_iter = 20
work_unit = "ciclo 9 iteracion 9/20: VEREDICTO INPUT-DEBUG — 13/30 vs 12/30 (+1, marginal). Techo I/O CONFIRMADO: feedback con input no converge en competitivo duro. El limite es del modelo, no del feedback. Siguiente: revertir input-debug (marginal) o mantener, y pulido / /compact"
max_auto = 20
target_iter = 20
awaiting_user = false
