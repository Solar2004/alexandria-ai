iter = 8
max_iter = 20
work_unit = "ciclo 9 iteracion 8/20: INPUT-DEBUG IMPLEMENTADO (feedback con input del test fallido). Corriendo N=30 para veredicto (results/codecontests30-inputdebug.out). Si mejora el 12/30, el mecanismo I/O existe; si no, confirmar limite y pasar a pulido"
max_auto = 20
target_iter = 20
awaiting_user = false
