iter = 15
max_iter = 20
work_unit = "iteracion 15/20: pulido final — escalada T3 del critic, evolve.detect continuo, alx-night en systemd real, dogfood feature end-to-end (alx genera y verifica un cambio del repo)"
max_auto = 20
target_iter = 20
awaiting_user = false
