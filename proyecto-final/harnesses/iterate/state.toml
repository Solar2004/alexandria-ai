iter = 7
max_iter = 20
work_unit = "ciclo 8 iteracion 7/20: dataset completo pool listo (bigcodebench-full.jsonl, 186 ejecutables). Reproducibilidad + metrica agregada + gate verde. Siguiente: run comprehensivo N=186 (2-3h) o CodeContests (familia 3) — REQUIERE /compact"
max_auto = 20
target_iter = 20
awaiting_user = false
