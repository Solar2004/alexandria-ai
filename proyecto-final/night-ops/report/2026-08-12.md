# Informe nocturno 2026-08-12 16:45

## Tarea 6: 6:- [ ] Revisar el repo AlexanderTheGreat y proponer 3 mejoras concretas de calidad (analisis, no cambios)
ESTADO: OK (rc=0)
 3.
Fix: `.cgr-*` en .gitignore + `git rm --cached .cgr-*` (local intacto).

**2. Backups con claves FUERA del repo** — `backup.sh` escribe `backups/atg-*.tar.gz` (220M c/u, 863M total) **dentro del repo**. El propio script avisa: "El tar contiene CLAVES (headroom, oc-go-cc)... Nunca lo subas a un repo". Depender solo de `backups/` en .gitignore = un `git add -f` de filtrar claves. Además, si un path del tar falta, `2>/dev/null || exit 1` falla con mensaje genérico sin decir cuál.
Fix: `OUT=~/backups/atg/...` (fuera del repo) + `*.tar.gz` en .gitignore como segundo seguro + backup.sh lista el path que falta.

**3. Drift README vs realidad + scripts sin verificación** — README documenta `config/ # settings base` pero `config/` está **vacío** (0 archivos trackeados); no menciona systemd/, plugins/, agents/, agents-volt/, night-ops/. `.claude/settings.json` vive solo en HOME (untracked), no versionado. Y 0 lint en repo: no hay shellcheck, no hay tests; solo `bash -n`/`py_compile` manual pasan.
Fix: actualizar árbol de README a dirs reales, marcar/borrar `config/` muerto, y añadir `scripts/lint.sh` mínimo (`bash -n` todos + `py_compile` .py + assert `.cgr-*` y `backups/` no trackeados).

### Pendiente
- No apliqué nada (tarea = proponer). Si quieres, ejecuto las 3 en commits separados.
- `install.sh:130` CYPHER_API_KEY = falso positivo (define KEY desde env, no secreto hardcodeado). `headroom.service` usa EnvironmentFile externo — sin secretos en git. Verificado.

## Resumen: 1 completadas, 0 fallidas
